# このBotについて
冷笑(🐟)するだけのBotです  
説明は要らないと思います
# 使うときには
ルートに新規で`config.json`と`uow_count.json`を作成してください。  
`config.json`の中身は以下
```json
{
  "token": "Bot_token" 
}
```
`uow_count.json`の中身は`{}`にしてください。  
起動は`npm run start`でできます。nodemonで起動するときは`npm run dev`を使ってください。
